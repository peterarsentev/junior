package ru.job4j.sort;

import java.util.Comparator;

public class HeighOrder {
    public static void main(String[] args) {
        Comparator<Integer> first  = (left, right) -> left - right;
//        first.thenComparing(null);

    }
}
